package validators

import "github.com/Appliscale/perun/logger"

func WarningMap(sink *logger.Logger, warnings ...string) {
	for _, warning := range warnings {
		if warning == "IsLonger64" {

		}
	}
}
